# 04 단계 — FastAPI Hello .env 예시
# 실제 .env 는 커밋 X (.gitignore 에 등록). 여기 값을 _수정_ 해서 .env 로 복사하면 됨.

APP_APP_NAME=fastapi-hello-dev
APP_VERSION=0.1.0
APP_ENV=dev
APP_HOST=127.0.0.1
APP_PORT=8000
APP_DEBUG=true
APP_LOG_LEVEL=DEBUG
APP_DOCS_ENABLED=true
