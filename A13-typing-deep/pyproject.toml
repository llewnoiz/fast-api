[project]
name = "typing-deep"
version = "0.1.0"
description = "A13 — Python 고급 typing & 메타프로그래밍"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/typingdeep"]
