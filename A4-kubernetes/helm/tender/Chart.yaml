apiVersion: v2
name: tender
description: tender FastAPI 서비스 — Helm chart
type: application
version: 0.1.0
appVersion: "0.1.0"

# 운영급 의존성 (이 chart 가 _bitnami 차트 사용_):
# dependencies:
#   - name: postgresql
#     version: 16.x
#     repository: https://charts.bitnami.com/bitnami
#   - name: redis
#     version: 20.x
#     repository: https://charts.bitnami.com/bitnami
